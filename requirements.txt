# Dava Araştırma + Blog sistemi Python bağımlılıkları
# Kurulu sürümlere pinlenmiştir (2026-06-02) — Süper Stajyer/CDP kırılmasını
# önlemek için upgrade churn'den kaçın.

# --- Runtime ---
playwright==1.58.0      # scripts/superstajyer.py (ASAMA 2A CDP otomasyonu)
python-docx==1.2.0      # scripts/md_to_docx.py (rapor/blog → DOCX)
PyPDF2==3.0.1           # scripts/pdf_inspect.py (müvekkil PDF inceleme)

# --- Dev / test (yalnız geliştirme; runtime gerektirmez) ---
pytest==9.0.2           # scripts/tests/ — trust-gate testleri
ruff>=0.6               # lint (WARN-only, yeni dosya + scripts/tests/ scoped)
